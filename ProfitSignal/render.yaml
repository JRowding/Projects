services:
  - type: web
    name: profitsignal
    runtime: python
    plan: free
    rootDir: ProfitSignal
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    healthCheckPath: /health
    autoDeploy: true
